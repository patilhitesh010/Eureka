// Frontend API Configuration
// Set API_BASE to your backend server URL (e.g. "https://eureka-startup-competition.onrender.com") when deploying the frontend on Vercel.
// If left as an empty string "", the frontend will use relative paths (proxying via vercel.json rewrites or same-domain hosting).
window.API_BASE = "";
